RewriteEngine On

# 设置目录索引文件
DirectoryIndex index.php index.html

# 如果请求的是实际存在的文件或目录，直接访问
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# 重写到index.php - 支持直接子页面访问
# 确保正确处理带有斜杠的路径
RewriteRule ^(.*)$ index.php?path=$1 [QSA,L]