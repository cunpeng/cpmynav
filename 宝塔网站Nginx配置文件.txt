打开宝塔面板
网站 → 设置 → 配置文件
在 server { ... } 块内， location / { ... } 之前添加：
# 保护敏感数据目录
location ~ ^/data/(.*) {
    deny all;
    return 403;
}

# 保护备份目录
location ~ ^/data/backups/(.*) {
    deny all;
    return 403;
}

# 保护配置文件（如果有暴露风险）
location ~ ^/src/config_fixed.php$ {
    deny all;
    return 403;
}
